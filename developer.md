# 👨‍💻 开发者指南 (Developer Guide)

本文档旨在帮助开发者快速上手本项目，理解核心架构，并掌握日常开发流程。

## 目录

1.  [环境准备](#1-环境准备)
2.  [启动开发环境](#2-启动开发环境)
3.  [项目架构概览](#3-项目架构概览)
4.  [核心功能开发](#4-核心功能开发)
    *   [添加新页面](#41-添加新页面)
    *   [数据库迁移 (Prisma)](#42-数据库迁移-prisma)
    *   [认证鉴权 (NextAuth)](#43-认证鉴权-nextauth)
5.  [常用命令速查](#5-常用命令速查)
6.  [部署指南](#6-部署指南)

---

## 1. 环境准备

确保你的开发机已安装以下工具：

*   **Node.js**: >= 18.17.0 (推荐使用 LTS 版本)
*   **npm**: 通常随 Node.js 一起安装 (或使用 pnpm/yarn)
*   **Git**: 版本控制工具
*   **VS Code**: 推荐 IDE，配合 Tailwind CSS 和 ESLint 插件体验最佳。

---

## 2. 启动开发环境

这是你每天工作的起点。`npm run dev` 命令会启动 Next.js 的本地开发服务器，支持热更新 (Hot Reloading)。

**如何操作：**

1.  打开你的终端 (Terminal) 或 VS Code 的内置终端 (`Ctrl + ~`)。
2.  确保你在项目根目录下 (即能看到 `package.json` 的目录)。
3.  输入以下命令并回车：

    ```bash
    npm run dev
    ```

4.  **看到什么代表成功？**
    终端输出类似以下内容时，表示启动成功：
    ```text
    ready - started server on 0.0.0.0:3000, url: http://localhost:3000
    ```
5.  现在可以在浏览器访问 [http://localhost:3000](http://localhost:3000) 查看前台，或 [http://localhost:3000/dashboard](http://localhost:3000/dashboard) 查看后台。

---

## 3. 项目架构概览

本项目采用 **Next.js App Router** 架构。

*   **`src/app`**: 路由核心。文件夹层级即 URL 路径。
    *   `src/app/(admin)`: 后台管理相关页面（带括号的文件夹不包含在 URL 路径中，用于组织代码）。
    *   `src/app/(auth)`: 认证相关页面 (如登录页)。
    *   `src/app/blog`: 前台博客展示页面。
    *   `src/app/api`: 后端 API 接口 (如 NextAuth 处理)。
*   **`src/lib`**: 通用工具库。
    *   `db.ts`: Prisma 数据库单例连接。
    *   `actions.ts`: Server Actions (处理表单提交等服务端逻辑)。
*   **`prisma/schema.prisma`**: 数据库模型定义文件。

---

## 4. 核心功能开发

### 4.1 添加新页面

在 `src/app` 下创建一个新文件夹，并放置 `page.tsx`。

例如，创建“关于我”页面：
1.  新建文件: `src/app/about/page.tsx`
2.  内容:
    ```tsx
    export default function AboutPage() {
      return <h1>About Me</h1>;
    }
    ```
3.  访问: `http://localhost:3000/about`

### 4.2 数据库迁移 (Prisma)

当你修改了 `prisma/schema.prisma` 文件后（例如给 User 表加了个字段），需要同步到数据库：

```bash
# 生成迁移文件并应用到数据库
npx prisma migrate dev --name add_new_field
```

如果只是想重新生成 Prisma Client 类型提示（没改数据库结构）：
```bash
npx prisma generate
```

### 4.3 认证鉴权 (NextAuth)

*   **配置文件**: `src/auth.ts` 和 `src/auth.config.ts`。
*   **中间件**: `src/middleware.ts` 负责拦截未登录用户访问 `/dashboard`。
*   **获取当前用户**:
    ```ts
    import { auth } from "@/auth";
    const session = await auth();
    console.log(session?.user);
    ```

---

## 5. 常用命令速查

| 命令 | 描述 | 使用场景 |
| :--- | :--- | :--- |
| **`npm run dev`** | **启动开发服务器** | **最常用！开发时一直开着。** |
| `npx prisma studio` | 打开数据库可视化界面 | 查看/手动修改数据库数据时使用。 |
| `npx prisma db seed` | 重置/初始化数据库数据 | 刚拉取项目或想恢复初始账号时使用。 |
| `npm run build` | 构建生产版本 | 部署前检查是否有编译错误。 |
| `npm run start` | 启动生产服务器 | 本地模拟生产环境运行 (需先 build)。 |

---

## 6. 部署指南

本项目推荐部署到 [Vercel](https://vercel.com)。

1.  将代码提交到 GitHub。
2.  在 Vercel 导入该仓库。
3.  **重要**：在 Vercel 项目设置中配置环境变量 (`Environment Variables`)：
    *   `DATABASE_URL`: 你的生产数据库连接串 (Vercel Postgres 或其他)。
    *   `AUTH_SECRET`: 生成一个随机字符串 (可用 `openssl rand -base64 32` 生成)。
4.  点击 Deploy。

---

🎉 **祝开发愉快！**
