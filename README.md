# ⚫️ MySpace - 极简多用户社交博客系统

MySpace 是一个基于 **Next.js 15** 构建的现代化、高性能个人空间与博客平台。它摒弃了繁琐的色彩，采用极致的**黑白极简主义设计**，专注于内容创作与用户间的纯粹互动。

## ✨ 核心功能 (Features)

*   **👥 多用户社交体系**：
    *   **个性化空间**：每个用户拥有独立的 `/space/[username]` 主页。
    *   **关注系统**：支持用户互关，实时更新粉丝与关注数，支持查看关注列表并跳转。
    *   **社交搜索**：支持精确搜索用户名，并内置基于数据库存储的**搜索历史记录**（保留 3 天）。
*   **📝 强大的内容创作**：
    *   **WYSIWYG 富文本编辑器**：集成 TipTap，支持**本地图片直接上传并即时预览**。
    *   **动态发布**：支持发表文字、图片、代码块等多种形式的“说说”。
    *   **文章管理**：支持单条编辑、单条删除，以及高效的**批量管理删除**功能。
*   **💬 深度互动反馈**：
    *   **通知系统**：实时消息提醒。当有人评论你的文章时，顶部导航栏会显示红色数字气泡。
    *   **锚点定位**：点击通知可精准跳转并滚动到对应评论的位置。
    *   **点赞与评论**：完善的点赞防重逻辑及评论管理（支持删除自己的评论或博主管理评论）。
*   **🎨 极致 UI/UX**：
    *   **黑白美学**：全站统一的硬朗黑白风格，配合优雅的淡入淡出动画。
    *   **个性化提示**：摒弃系统默认弹窗，采用自定义的黑白风格 Toast 和 对话框。
    *   **响应式适配**：完美适配手机、平板、桌面端，提供流畅的跨端阅读体验。

## 🛠️ 技术栈 (Tech Stack)

*   **框架**: [Next.js 15 (App Router)](https://nextjs.org/)
*   **语言**: TypeScript
*   **样式**: Tailwind CSS + [Shadcn/ui](https://ui.shadcn.com/)
*   **数据库**: SQLite + [Prisma ORM](https://www.prisma.io/)
*   **认证**: [Auth.js (NextAuth v5)](https://authjs.dev/)
*   **编辑器**: TipTap (Rich Text + Image Upload)
*   **图标**: Lucide React + IcoFont
*   **组件**: Sonner (Toast), Framer Motion (Animations)

## 🚀 亮点 (Highlights)

1.  **所见即所得**：上传图片不再显示冷冰冰的链接，而是直接在编辑器内显示真实图片，发布流程极其直观。
2.  **毫秒级响应**：基于 Next.js Server Components，页面加载速度极快，交互反馈灵敏。
3.  **沉浸式体验**：全局禁止文字选中光标闪烁，结合 1s 的优雅淡入动画，打造应用级的沉浸感。
4.  **安全可靠**：完善的权限校验，确保用户只能编辑/删除属于自己的内容。

## ⚙️ 环境准备

*   **Node.js**: >= 18.17.0
*   **Package Manager**: npm, yarn 或 pnpm

## 📦 快速开始与配置

### 1. 克隆项目
```bash
git clone https://github.com/your-username/personal-blog.git
cd personal-blog
```

### 2. 安装依赖
```bash
npm install
```

### 3. 环境配置
在项目根目录创建 `.env` 文件，并填入以下内容：
```env
# 数据库连接串
DATABASE_URL="file:./dev.db"

# Auth.js 密钥 (可用 openssl rand -base64 32 生成)
AUTH_SECRET="your_random_secret_here"
```

### 4. 初始化数据库
```bash
# 同步数据库结构
npx prisma db push

# 生成 Prisma Client
npx prisma generate
```

### 5. 启动开发服务器
```bash
npm run dev
```
访问 [http://localhost:3000](http://localhost:3000) 即可开始。

## 📄 开源协议

MIT © [Denght123]
